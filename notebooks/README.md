# Notebooks

Exploration only. Nothing load-bearing lives here — analysis logic belongs in `src/inhibitome/`
and the numbered `scripts/`. Notebooks are for looking at data, not for results that go in the paper.
